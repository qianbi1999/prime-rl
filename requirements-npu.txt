# Minimal additional dependencies for prime-rl RL training on Ascend NPU
# Base image: quay.io/ascend/vllm-ascend:v0.20.2rc1-a3
# (already includes torch 2.10.0+torch_npu, vllm 0.20.2+vllm_ascend, transformers, triton_ascend, CANN 9.0.0)
#
# CUDA-only packages deliberately EXCLUDED:
#   liger-kernel, flash-attn*, ring-flash-attn, flash-linear-attention,
#   nvidia-ml-py, mooncake-transfer-engine, deep-ep, deep-gemm, tilelang

# --- core runtime ---
beartype>=0.21.0
jaxtyping>=0.3.2
pyarrow>=21.0.0
datasets>=4.0.0
torchdata>=0.11.0

# --- orchestrator / networking ---
aiolimiter>=1.2.1
tenacity>=8.2.0
orjson>=3.11.0

# --- logging / monitoring ---
wandb>=0.26.1

# --- wordle environment dependencies ---
prime>=0.6.4
nltk>=3.9.2
textarena==0.7.4
